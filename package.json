{
  "name": "klondike-protocol-v2",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "hardhat node",
    "test": "hardhat test",
    "compile": "hardhat compile",
    "coverage": "hardhat coverage",
    "deploy:kovan": "rm -rf ./artifacts && hardhat compile && hardhat deploy --network kovan",
    "verify:kovan": "rm -rf ./artifacts && hardhat compile && hardhat contract:verify:all --network kovan",
    "front:kovan": "hardhat abi:generate --network kovan",
    "deploy:mainnet": "rm -rf ./artifacts && hardhat compile && hardhat deploy --network mainnet",
    "verify:mainnet": "rm -rf ./artifacts && hardhat compile && hardhat contract:verify:all --network mainnet",
    "front:mainnet": "hardhat abi:generate --network mainnet"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.0",
    "@nomiclabs/hardhat-etherscan": "^2.1.1",
    "@nomiclabs/hardhat-waffle": "^2.0.0",
    "@types/chai": "^4.2.14",
    "@types/mocha": "^8.2.0",
    "@types/node": "^14.14.22",
    "chai": "^4.2.0",
    "ethereum-waffle": "^3.0.0",
    "ethers": "^5.0.0",
    "hardhat": "^2.0.8",
    "hardhat-gas-reporter": "^1.0.4",
    "solc": "0.6.6",
    "solidity-coverage": "^0.7.14",
    "ts-node": "^9.1.1",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "@chainlink/contracts": "^0.1.6",
    "@openzeppelin/contracts": "^3.3.0",
    "@uniswap/lib": "^4.0.1-alpha",
    "@uniswap/v2-core": "^1.0.1",
    "@uniswap/v2-periphery": "^1.1.0-beta.0",
    "axios": "^0.21.1",
    "dotenv": "^8.2.0",
    "synthetix": "^2.37.0"
  }
}
